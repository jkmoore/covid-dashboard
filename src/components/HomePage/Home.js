import "../../App.css";
import React from "react";

export const Home = () => {
    return(
        <main className="container">
            <h2 className="header">Home: Maybe a layout (graph, chart, or table) of data for the whole country</h2>
        </main>
    );
}